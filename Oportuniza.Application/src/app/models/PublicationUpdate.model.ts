export interface PublicationUpdateDto {
  title: string;
  description: string;
  salary: string;
  shift: string;
  contract: string;
  local: string;
  expirationDate: string;
  authorUserId: string;
}
