import { SafeImagePipePipe } from './safe-image-pipe.pipe';

describe('SafeImagePipePipe', () => {
  it('create an instance', () => {
    const pipe = new SafeImagePipePipe();
    expect(pipe).toBeTruthy();
  });
});
